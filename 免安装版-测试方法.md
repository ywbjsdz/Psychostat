# 免安装版：测试与验收方法

> 给收到「Psychostat 免安装版」压缩包的人。
> 目标：**10 分钟内确认工具能在他自己的电脑上正常工作**，如果不行，把关键信息回传。
> 本文分三档，从最快到最全。**至少做第 1 档。**

---

## 0. 前置：解压位置（很重要）

把压缩包里的两个文件夹解压到 **D 盘根目录**，最终必须是：

```
D:\Psychostat          ← 工具本体（双击这个文件夹里的 启动Psychostat界面.bat）
D:\Psychostat-R        ← 随包附带的 R 运行时，工具会自动认到
D:\Psychostat-Python   ← 随包附带的 Python 运行时（生成 Word/HTML 报告用），工具会自动认到
```

- **不要**放进 OneDrive、桌面或很深的目录（路径过长/被同步会出问题）。
- **不要**只解压出内层文件夹（正确解压后 D 盘应该有且仅多出上面两个文件夹）。
- 若电脑**没有 D 盘**：解压到 C 盘也行，但首次启动工具会提示找不到 R，
  此时按第 3 档「无 D 盘怎么办」处理。

---

## 1. 第 1 档：离线自检（约 1–3 分钟，必做）

双击（或在命令行运行）：

```
D:\Psychostat\运行离线自检.bat
```

它用**随包的 R** 跑 88 条统计与 IRT 断言，不需要联网。

**通过的样子**（最后几行）：

```
[PASS] 生产代码：Type III SS_A（非平衡 2×2）
...
[PASS] IRT端到端：CIFA 的 BIC 不得差于单维（防模型语法错误退化成差模型）
ALL NUMERIC TESTS PASSED

  RESULT: ALL NUMERIC TESTS PASSED
  The tool and its bundled R are working correctly.
```

**如果看到 `RESULT: TESTS FAILED`**：把整个窗口的文字复制下来回传即可（这是最有用的诊断信息）。

> 这一档能同时验证三件事：随包的 R 能启动、R 包一个不缺、统计核心算得对。
> 它**不依赖联网、不依赖 Python、不依赖 Power**。

### 常见拦截（不是故障）

| 现象 | 处理 |
|---|---|
| 弹「Windows 已保护你的电脑」 | 点「更多信息」→「仍要运行」。原因是脚本没有代码签名 |
| 杀毒软件报警 | 放行即可。这个包**不联网上传任何数据**，只在你本机跑统计 |
| 双击 .bat 一闪而过 | 本包的 .bat 失败时会暂停显示原因；若仍闪退，改用命令行手动运行看报错 |

---

## 2. 第 2 档：功能抽查（约 10 分钟）

确认"真的能出结果"，而不只是自检通过。

**第 1 步：图形界面能不能起来**

双击 `D:\Psychostat\启动Psychostat界面.bat` → 应该出现向导窗口，左侧有
「心理统计 / 经典测量理论 CTT / 项目反应理论 IRT」三条分支。
顶部/左侧会显示 R 是否就绪（应该显示已就绪，因为随包带了 R）。

**第 2 步：跑一个内置演示（不需要你自己的数据）**

在界面里：选「心理统计」→ 数据来源选「内置演示」→ 方法勾选 `描述统计` + `独立样本 t 检验`
→ 跟着步骤走 → 开始分析。

几分钟后底部状态栏会出现结果目录，点「打开结果文件夹」应能看到：

- `NN_方法_*.csv` —— SPSS 式结果表（可直接导入 SPSS 对照）
- `*.png` —— 300 dpi 图
- `stats_report_zh.md` —— 中文教学报告（什么时候用 / 怎么点 / 怎么读 / 怎么写）
- `run_manifest.json` —— 本次运行的 R 与包版本、随机种子（复现用）

**第 3 步：CTT 与 IRT 各跑一次**

- 「经典测量理论 CTT」→ 内置模拟 → 应产出清洗、项目分析、信度（α 与 ω）、EFA/CFA 结果。
- 「项目反应理论 IRT」→ 使用自带的模拟数据 → 应产出模型比较、题目参数、能力估计、ICC/IIF/TIF 图。

**第 4 步（可选）：用自己的数据**

支持 CSV / XLSX / SAV。注意：**一个被试一行、一个题目一列**。
把列名直接粘进界面即可；若列名是中文或含空格，工具内部会做安全处理（不会算错）。

**第 5 步：Word 报告（可选，需要联网）**

Word 结果报告依赖 Python。界面会提示是否自动安装（约 25 MB）。
**不装也不影响** CSV 表格、图和 Markdown 报告。

---

## 3. 第 3 档：完整验收（研究者 / 想确认统计口径）

**A. 与自己的 SPSS 结果对照**

每个方法都给了 SPSS 菜单路径。把结果目录里的 `NN_方法_data.csv` 导入 SPSS
（UTF-8-BOM 编码，可直接打开），按提示的路径点一遍，统计量应与 CSV 表对到小数点后 2–3 位。

> 已知差异（**不是错误**，我们在文档里主动写明）：
> 混合设计有 3 处未逐位对齐 —— EM 边际均值的标准误、被试内配对比较的误差项、
> Huynh-Feldt ε（本工具 0.984 vs SPSS 1.000，两者校正结论一致）。
> 另有 Mauchly 卡方 5.141 vs 5.145（第 3 位小数），W/df/p 三项一致。
> 详见 `SPSS与Psychostat心理统计模块输出对应.docx` 开头的「校对范围与已知差异」。

**B. 复现性检查**

同一份数据、同一个种子重跑一次，结果应完全一致。
每个结果目录里的 `run_manifest.json` 记录了：时间戳、分支、R 版本、所有 R 包版本、随机种子、输入路径。
把这份 JSON 连同结果一起存档，将来能精确复现。

**C. 大样本与边界自检**（可选，验证我们修过的坑）

1. 拿一份 **N > 5000** 的数据做正态性检验 → 应看到结论里明确写着
   **「未检验」**（S-W 在大样本下不适用），而**不是**"正态性通过"。
2. 拿一份**组间方差不齐**的数据做单因素 ANOVA → Word 报告里应出现
   Levene 警示，并列出 Welch / Brown-Forsythe 稳健检验结果。
3. 列名故意写成 `T1-T3总分` 这类含连字符的 → 工具应在日志里提示
   "已改用安全名计算"，且结果**不应**报错或给出奇怪的模型。

**D. 反馈模板**（回传时请附上）

```
系统：Windows ____ / 是否有 D 盘：是/否
解压位置：D:\Psychostat + D:\Psychostat-R
第 1 档结果：ALL NUMERIC TESTS PASSED / TESTS FAILED（附窗口文字）
第 2 档：界面能起 / 演示能跑完 / 结果目录里有 ____
出现的问题（截图或文字）：
想拿它做什么：作业 / 毕业论文 / 量表编制 / 其它____
```

---

## 4. 排错速查

| 现象 | 原因与处理 |
|---|---|
| 提示找不到 R，还问要不要自动装 | 没解压到 `D:\Psychostat-R`，或解压位置不对。回到第 0 节检查目录结构 |
| 「compiler 命名空间不可用」 | R 核心异常。先确认 `D:\Psychostat-R\bin\Rscript.exe` 存在且不残缺（重新解压一次） |
| 界面能起但点分析没反应 | 看界面里的日志区；也可以在 `D:\Psychostat` 打开 PowerShell 跑 `.\run_psychostat.ps1` 选 1 看详细报错 |
| Word 报告没生成 | 没装 Python。按提示装一次（需联网），或忽略——CSV/图/Markdown 报告不受影响 |
| 结果目录越积越多 | `D:\Psychostat\outputs\` 下每次运行一个时间戳目录，可随时删旧的不影响工具 |
| 不想用了 | **直接删掉 `D:\Psychostat` 和 `D:\Psychostat-R` 两个文件夹**即可，不留残留。工具没有装任何东西到系统里 |

---

## 5. 一句话总结

> 解压到 D 盘 → 双击 `运行离线自检.bat` 看到 **ALL NUMERIC TESTS PASSED** → 双击
> `启动Psychostat界面.bat` 跑一次内置演示 → 就说明这套东西在你这儿是好的。
