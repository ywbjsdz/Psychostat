# IRT-A：二分 2PL 数据（800 人 × 20 题，真值 a∈[0.8,2.0]、b∈[-2,2]）
# 用法：Rscript scripts/irt_generic_pipeline.R --config examples/simulated_datasets/<本文件>
input:
  mode: file
  path: "./irt_A_binary_2pl_n800_items20.csv"
  item_columns: auto
  missing: none            # none 与 pairwise 等价；listwise 会先删人
analysis:
  model: auto
  mirt_mode: eifa
  selection_criterion: BIC
  compute_m2: true
output:
  directory: "../../outputs"
  report_languages: [zh, en]
  save_model_object: true
