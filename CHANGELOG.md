# 更新日志（Changelog）

本项目的所有显著变更都记录在此文件中。
格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### 修复（早期试用反馈）：调节效应的 HTML/Word 报告会整份崩掉

**现象**（早期试用者实际遇到）：跑完分析后生成 HTML 报告时报
`UnboundLocalError: cannot access local variable 'p1' ...`，**整份报告都出不来**；
而另一位朋友同样的包能正常出网页——说明与机器无关，是特定数据路径触发。

**根因**：`scripts/generate_stats_report.py` 的 `s_moderation()` 里，
`p1`/`p3s` 只在 `if not sl.empty:` 分支内赋值，却被后面的句子无条件引用
（同一处的 `s1`/`s3` 有预初始化 `s1 = s3 = None`，`p1`/`p3s` 被漏了）。
触发条件是 **`moderation_simple_slopes.csv` 缺失或为空**——而 R 侧
`save_table(slopes, ...)` 是**无条件**写出的，所以"有交互模型表、没有斜率表"只可能来自
**该模块跑到一半失败**：`stats_pipeline.R` 的设计是"单个方法失败不中止整次运行，
记录原因并在报告该节标注"，于是留下半成品目录，随后被报告生成器放大成整份失败。
已用"删掉斜率表"的方式在本机**逐字复现**了该 traceback（`generate_stats_report.py:360`）。

**修法（两层，均在报告层，不动统计计算）**
1. `s_moderation()`：补上 `p1 = p3s = None`；并在斜率表缺失时**明说**
   "简单斜率表未生成或为空，本节无法给出各条件下的效应；交互项系数与结论不受影响"，
   而不是印出 `NA` 或崩掉。
2. `build_registry()` 统一加保护层 `_guarded()`：任何方法的**段落生成**抛异常时，
   只跳过该节文字、在报告里插入一条显式说明，并把原始 traceback 打到 stderr；
   统计表照常输出。Word 与 HTML 两条路径共用本注册表，因此一次修复覆盖两边。
   动机：一处小缺失不该让用户看到与真正原因相距很远的整份失败。

**验证**：① 删除斜率表 → 报告正常生成且含缺表说明；② 恢复斜率表 → 报告数值与 CSV 一致
（低端斜率 1.109、高端 5.477，正文均命中）；③ 人为把某方法换成必抛异常的实现在注册表上调用 →
保护层返回兜底说明且打印 traceback；④ 冒烟测试（含文件编码约定与心理统计报告生成）全部通过。

> 排查提示（给试用者）：若某方法没跑完，**先看结果目录里的 `stats_report_zh.md`**——
> R 侧会把失败原因写在该节；`run_log.txt` 里也有对应记录。

### 校对（CTT / IRT 输出独立复核，2026-09-18）

**结论：未发现计算错误。** 三层判据全部实跑通过，**FAIL 均为 0**：
CTT 的 L1 复算 61 项、L2 真值恢复 4 项；IRT 的 L1 复算 109 项、L2 真值恢复 32 项、L3 独立实现对照 6 项。
完整实测数据见 「输出校对记录」，方法与清单见 「输出校对方案」。

判据分层：**L1** 用教科书闭式从原始数据/参数表重算每一个量（不经过被测代码）；
**L2** 用模拟数据生成时的真值检验估计（能发现方向反了、标签错配）；
**L3** 换另一个作者、另一种算法的 R 包重跑（能发现闭式复算发现不了的"共同实现错误"）。
范围按需求定为**不对 SPSS、也不对 Mplus**；L3 用 `ltm`（Rizopoulos 2006）作第二实现。
之所以必须这样分层：工具的引擎本身就是 `psych`/`lavaan`/`mirt`，用它们去"验证"自己是同义反复。

**新增校对脚本（5 个，独立于被测实现）**

- `tests/verify_l1_ctt.R`：α、删除项后 α、CITC、CR 决断值（27% 分组 + 合并方差 t）、KMO、
  Bartlett（χ² 与 df）、EFA 表内部一致性、CFA 的 CR/AVE、单因子 ω、α 的置信区间（对照 Feldt 1965 精确式）、
  用 `stats::factanal` 独立复核 EFA 结构；以及**清洗计数与个案级 `removed` 标记的逐行独立复现**
  （5 个目录、400–800 行，最大偏差 0、不一致 0 处）。
- `tests/verify_l1_irt.R`：MDISC=sqrt(Σa²)、b↔d 换算（含 GPCM 的台阶口径）、ICC 用
  Samejima/Muraki/Birnbaum 闭式逐点复算、IIF=Σ(P′)²/P、TIF=ΣIIF、模型比较表与 `Selected`、
  CIFA 载荷矩阵结构、缺失审计勾稽、Rasch 的 a 极差、题目拟合的 BH 校正、
  **各输出表的题名口径一致性（新增 I12，正是它发现了下面第 1 条缺陷）**。
- `tests/verify_recovery_ctt.R` / `tests/verify_recovery_irt.R`：L2 真值恢复。题目按**数字键**
  （`Item01 ↔ Item1`）对齐并**断言逐题全覆盖**，对齐不足即判 FAIL——防止题名口径不一致时
  只对上半个数据、却报"恢复良好"。
- `tests/verify_l3_irt_ltm.R`：与 `ltm` 的独立实现对照。实测 2PL 的 a/b 与 GRM 的 a/4 个阈值
  **相关全部为 r = 1.0000**，|平均偏差| ≤ 0.008。
  （口径：`ltm` 只覆盖单维 2PL/GRM，故只在 2 个可对照目录上执行 → PASS=6/SKIP=0；
  把 8 个目录一并传入则另产生 SKIP=6。）

**第三方独立复核（2026-09-18）**

第二轮（跨模型交叉验证）的提示词已备好：第二轮复核提示词（存档于本地）。
第二轮**换靶子**而不是重复第一轮：G1 多维 GPCM 的 L3 独立对照（`ltm` 只覆盖单维，
多维 GPCM 至今无第二实现）、G2 CFA 的 L3（`OpenMx`/`semTools`，需先对齐 WLSMV/ML 与 std.lv）、
G3 `M2*`/`SRMSR`/`TLI` 的第二实现或文档级核对、G4 **报告层忠实性**（CSV ↔ md/docx/html，
至今无人查过，是"会写进论文"的最后一道关口）、G5 `mirt::empirical_rxx` 与近似式的口径差异考证。
提示词含**读取白名单**——仓库 `outputs/` 下有 286 个结果目录、7994 个文件、870MB，
而本轮真正需要读的只有 5 个源码 + 16 个数据/配置文件 + 5 个指定结果目录；
并把第一轮的教训写成硬性要求：**复算脚本必须留在仓库**，否则该条结论按"未审计"处理。

把本记录与全部校对脚本交给外部模型（ZCode／内置 GLM-5.3）做**独立复核与证伪**，提示词见
复核提示词（存档于本地）。对方走 **A 实跑**模式（自写 250 行独立复算器、
Richardson 外推作为第三种求导路径、回放我方脚本、`git diff` 审计、边界矩阵与坏配置注入），
判定 **50 条主张全部一致、无高/中severity 问题**，并用 `ltm::tpm` 补做了我方未做的 3PL 猜测参数对照。
它对我方**校对脚本**提出 5 条低severity 指控，逐条复核**全部属实**，已修（均在 `tests/` 与 `docs/`，
**未动核心代码**）：

1. **I10 近恒真**：守卫条件 `max|a−1| < 1e-8` 与断言是同一件事 → 工具若把 Rasch 拟合成别的模型，
   只会 SKIP 永不 FAIL。改为按**配置声明的模型**判定，失配即 FAIL。
2. **I5 循环内残留静默 `next`**（TIF 有某维度而 IIF 无时跳过、不计失败）→ 改为记为失败并报出维度与 θ。
3. **L2 的 MIRT-CIFA θ 按位置配对**，隐含"工具维度编号 = 真值维度编号"→ 把前提显式化，
   先用载荷矩阵验证同序，否则明确报"未判定"。
4. **校对方案 A6 的阈值放宽未声明**（"相关 > .95" → "比值∈[0.75,1.25] 且 r ≥ .70"）→ 已声明并给出理由。
5. **L3 的"SKIP = 0"未说明口径** → 已补（2 目录 vs 8 目录）。

修后复跑 L1 = 109/0/13、L2 = 32/0/0、L3 = 6/0/0，与修前一致：修的是**判别力与文档口径**，
未改变任何数值结论。另外，`ltm::tpm` 的补充对照显示两个独立实现互相吻合（c 的 r = .9755）
**却都恢复不了真值**（−.41 / −.36），因此该证据只支持"无实现缺陷"，
**不能**推出"逐题猜测参数可用"——已在校对记录 3.1 按两面记录。

**第二轮第三方复核（Codex／GPT-5.6 Terra，换靶子）**

第二轮只攻击第一轮未覆盖的 5 个空白，报告见 `docs/第三轮_CTT与IRT审查报告.md`，
审计脚本 独立复算脚本（含 results/ 与 sessionInfo，满足可审计性要求）。

结果：**G4 报告层忠实性 15/15 一致、G5 `empirical_rxx` 口径确认无误**（这两块是新增证据）；
**G1 多维 GPCM 的独立 L3 未完成、G2 partial（同估计器复现）、G3 无法判定**。
我方逐条核验了它报告的每一个数字（ctt_B 清洗 500→475、G5 四个 Empirical_rxx 与近似式差、
G4 的 BIC/α/KMO/p、G2 的 4.44e-16），**全部吻合**；它未改核心代码、未污染 outputs/。

**它对唯一"中等发现"的定性被我方改判**：它报告"IRT 输出未保留模型对象，导致拟合指标无法独立
审计"，归类为工具设计缺陷。实际是——① 工具**默认就保存**（`save_model_object %||% TRUE`，
`irt_generic_pipeline.R:270`），所有模拟配置均为 true 且这些目录里确实有 `*_selected_model.rds`；
② 它检查的三个目录没有 `.rds`，是因为那三份冒烟夹具显式设了 `false`，而这三份夹具是我方在
第二轮白名单里**为 G4（需要 docx/html 报告）挑的**——挑 G4 的材料把 G3 的材料挑没了，
属我方材料清单失误；③ 且它建议的补救（保存对象后重算 M2*）与它自己写的"不能把同一 mirt 路径
重跑称为独立复算"自相矛盾，独立复算必须在第二引擎重拟合。故此项**不是产品缺陷**。

另附我方对 G1 的可行性实测：`TAM::tam.mml.2pl(irtmodel="GPCM")` 在 1 维×8 题为 0 秒、
2 维×16 题（n=200）为 2 秒成功，但 3 维×24 题在 n=150 上 380 秒未完成——
多维 GPCM 在 TAM 里"2 维可行、3 维不收敛"。第三轮两条路：装 `sirt`，或在 2 维子模型上做对照。

**第三轮第三方复核（GPT-5.6 Terra 补作业轮，2026-09-18）**

按 第三轮复核提示词（存档于本地） 只做 G1–G3。报告：
补充复核报告（存档于本地）；脚本与证据：复核证据（23 个文件）。
复核方**正式撤回**第二轮那条"IRT 未保留模型对象"的发现，接受我方归因；
至此项目记录中**不存在未解决的第三方发现**。

结果：
- **G1 单维标定**（同一 GPCM 模型两引擎）：a r=.999996、四个台阶 b_d r=.999997–1.000000；
- **G1 二维路线**（F1+F2，16 题，同一样本 n=900）：量尺锚定后 a r=.997669、台阶 r=.999654–.999969，
  但 b_d4 平均差 **.05132** 越过 .05 建议线 .00132 → 复核方如实标为"一致（边界）"、不掩盖、
  也不诊断为产品错误；
- **G2 有序 CFA 的独立 L3**：未完成，**已查明原因**（见下）；
- **G3 同一 2PL 的拟合指标**：mirt 与 TAM 的 SRMSR 相差 **5.04e-05**；M2*/df/TLI/CFI
  在 TAM 侧不存在 → 判为"不可比"。

**方法上值得留存的两点**（已写入校对记录 §12.3）：① **先标定、再比对**——先用单维 GPCM
把参数化换算钉死（`a_ij=B_{i,1,j}`、`b_ik=(AXsi_{i,k}−AXsi_{i,k−1})/sqrt(Σ_j a_ij²)`），
换算没对上就不进二维；② **量尺锚定**——比较前用零截距最小二乘对齐潜在量尺
（`a_mirt=0.9563302×a_TAM`），避免把识别尺度差异误判成产品参数差异。

**由此确定并写入用户手册的三条"未独立验证区域"**（均为已分析过的技术原因，不是遗漏）：
1. **三维及以上多维 GPCM**：TAM 在 3 维×24 题下不收敛（n=150 时 380 秒未返回），2 维可秒级完成；
   补齐需 `sirt` 等可收敛的第二引擎。
2. **有序题 CFA**：`OpenMx::mxFitFunctionWLS(type=)` 只有 `WLS/DWLS/ULS`（已在本机核对函数签名），
   没有 lavaan WLSMV 的均值/方差修正；其有序路径是 raw-ordinal ML，属**不同估计量**；
   `semTools` 是 lavaan 插件、**不构成独立引擎**。故 CFA 只有 L1+L2，无 L3；
   若要 L3 只能改走"两侧同为连续 ML"的口径并注明与 WLSMV 不完全可比。
3. **`M2*`/df/`TLI`/`CFI`**：TAM 不输出，只有 SRMSR 可数值对照；SRMR 两引擎定义不同、不可逐位比。

这三条已同步写入 `docs/Psychostat_完整使用说明书.md`（§11.1 第 10 条）、
`docs/CTT使用说明.md`（§9.1）、`docs/IRT工具使用说明.md`（§4.2），
读者不必翻内部记录即可知道边界；校对记录新增 §12（含三轮总账），并改写了 §9 第 1 条
（把"时间不足未做"改为"已查明不可得及其原因"）。

> 复核方自述：其复算器自己先修了 6 个 bug 才得到 157/157（多维 ICC 取数、CTT 矩阵子集化、
> C10 插补时序、I10 未设门、Rasch 固定参数未特判、正则转义），并披露 I6 与 CTT-B3 只回放了我方脚本、
> 未独立重算。**其核心数字无法被审计**：复算脚本随临时目录一并删除，
> 故"157/157"按"未审计"看待；下一轮提示词已加入"复算脚本必须留在仓库"的硬性要求。

**修复（本次校对发现并修掉的问题，均不涉及统计公式）**

1. **CIFA（多维验证性）路线的题名口径不一致**。走 CIFA 时 `icc_data.csv`、`iif_data.csv`、
   `item_fit.csv`、`cifa_loading_significance.csv` 写的是内部安全名 `Item1`、`Item2`…，
   而 `item_parameters.csv` 等写的是用户原始题名 `Item01`、`Item02`…，**同批输出无法按 `Item` 直接 join**
   （做题名筛选或批量合并时会静默丢题）。根因：CIFA 为让 mirt 语法合法会把题名换成 `Item1…`，
   参数表在写出前翻回了原名，但绘图函数、显著性表、题目拟合表漏了这一步。
   修法是这几处统一套用 `cifa_to_orig_item()`（该函数在非 CIFA 路线是恒等映射，
   因此**其余路线的输出逐字节不变**）。顺带修正：`item_fit.csv` 的 `Item` 列原本取 `rownames()`，
   而 mirt 的 `itemfit()` 行名是行号（"1"、"2"…）——这一列从来不是题目名，现改为与 `item` 列同为原始题名
   （列仍保留，不改变输出结构）。
2. **IIF（题目信息量）的数值微分精度不足**。`mirt::probtrace()` 不提供解析导数（已核对 `args()`），
   只能用 θ 网格数值微分；原实现是 3 点格式（截断误差 O(h²)），实测最大偏到峰值的 2.7%。
   改为 4 阶格式（内部 5 点中心差分 + 两端 4 阶单侧差分）后，8 条路线全部降到**峰值的 0.00%**
   （最大绝对差 2.997e-07 ～ 3.40e-05）。这是同一公式的更精确数值实现，不是公式改动。
3. **小样本警告写在了错误的分支里**。`irt_preflight.R` 的"样本量仅 N：IRT 参数估计极不稳定"
   被写在 `if (requested_model == "mirt")` **内部**，于是只有多维路线才提示；而单维 2PL 每题还要
   多估一个区分度，样本量敏感性并不更低。现移到该分支**之外**，对全部 IRT 路线生效。
   实测：n=40 的 2PL 现在会提示；n=80 的 GRM 会提示"模型参数较多"；n=400 不提示。
4. **CTT 反向计分缺少观测范围校验**（对应 `scripts/ctt_pipeline.R`）。反向计分前先核对观测取值范围
   与 `cleaning.scale_maximum` 是否自洽：不一致时直接报错，而不是按要求静默按 `k+1−x` 算出**全错**的
   反向分。实测 0–4 编码却声明 `scale_maximum: 5` 时报错拦住，并在 `-Verbose` 的原始诊断里给出原因。
5. **中位数插补用了错误的分母**（对应 `scripts/ctt_pipeline.R`）。插补值原先按**全样本**中位数计算，
   而高缺失个案随后就会被整行删除——等于用一批不会进入分析的人去决定进入分析的人的取值。
   现改为只用"会被保留下来"的个案计算。
6. **静默模式下 R 的诊断文字不可见**（对应 `run_ctt_analysis.ps1`）。非静默模式原本只回显
   `Detected/Fitting/…` 之类的英文进度行，把"注意：以下列零方差…已排除在题目之外"这类
   关键中文提示全部丢掉。现补一条回显规则（只显示，不改变任何计算）。

**新增边界回归夹具（已接入 `tests/smoke_test.ps1 -Full` 第 6 节）**

| 夹具 | 触发条件 | 断言的可观察行为 |
|---|---|---|
| `tests/e2e_ctt_zero_variance.{yaml,json}` | 10 题中有 1 道零方差题 | R 层出现「零方差」提示并点名 `Item10`；清洗后恰剩 9 题、`Item10` 不出现 |
| `tests/e2e_ctt_wrong_coding.{yaml,json}` | 0–4 编码却声明 `scale_maximum: 5` 且要求反向计分 | 启动器非零退出；`-Verbose` 原始诊断含 `Reverse scoring assumes 1..scale_maximum` |
| `tests/e2e_ctt_median_scope.{yaml,json}` | 40 名高缺失个案（终将被删）+ 60 名完整个案，其中 P041 的 `Item06` 缺失 | 保留 60 人；**P041 的 `Item06` 插补为 4**（保留者中位数）而非 3（全样本中位数） |
| `tests/e2e_irt_small_n.{yaml,json}` | n = 40、10 道二分题、2PL | 体检事件的 `warnings` 含「样本量仅 40」 |

**记录在案、本次未改的两项**

- **静默模式下 R 的诊断文字只进 JSON 日志、不落盘到 `run_log.txt`**（零方差列的排除提示即是一例，
  静默模式下只能用 `-Verbose` 取回）。保持现状的理由：工具的设计约定是"静默 = 只出 JSON"，
  改动它属于产品行为变更，超出本次校对范围。
- **CFA 的 L3 独立复核（`OpenMx`/`semTools`）与 `TAM`/`psych::irt.fa` 对照尚未做**，
  缺口已在 「输出校对记录」 第 9 节逐条列出。


### 新增

- **IRT 说明书改为 Markdown 源**：`docs/IRT工具使用说明.docx`（8500 字、10 章）此前**只有 Word、没有源文件**，
  改代码必须手工同步 Word——模型表因此一直停在 GRM、缺 GPCM/MGPCM/M2PL，是它反复「过期」的根源。
  现转为 `docs/IRT工具使用说明.md` 纳入版本管理，并补齐 GPCM 相关内容（模型表补 GPCM 与
  `mirt_item_model` 五种取值、b 的台阶口径、向导的 3–5 问与多维家族、3PL 的 c 弱识别警示，
  新增「4.1 几条实测经验」）。Word 版改由 `scripts/merge_manuals.py` 在打包时生成；
  该脚本里只服务于旧 docx 的 `add_docx_content()` 已一并删除（死代码）。
  同时删除已过期的合并版产物 `Psychostat使用说明.docx`（打包时重新生成，不入库）。

- **IRT 模型选择向导**（GUI 模态对话框 + 终端问答，两处同口径）：按 计分类型（二分/多级）→ 维度（单维/多维或不确定）→ 样本量 →（多级）作答方式/（二分）特殊情况 四问推荐 Rasch/2PL/3PL/GRM/GPCM/MIRT，附推荐理由与文献口径（de Ayala《The Theory and Practice of IRT》2022：Rasch 百级可稳、2PL 约 250-500 起、3PL 猜测参数需 N≥1000；Dai & Chang 2021, Frontiers in Education：多级 ≥250-500、样本够时 GRM/GPCM 双模型 BIC 比较）。GUI 采用推荐后自动回填模型下拉框；终端向导会同时设置 BIC 比较候选集。
- **GPCM 模型支持**（`mirt` itemtype="gpcm"，Muraki 1992 相邻类别 logit）：单维 `model: gpcm` 与多维 `mirt_item_model: gpcm` 均可用；多级数据自动推荐在 N≥500 时同时拟合 GRM 与 GPCM 用 BIC 选优；二分数据选 gpcm/grm 会被友好拦截。参数提取（a/d 与 b 阈值）、ICC/IIF/TIF 绘图、项目拟合、能力估计对 gpcm 全部适用（经 probtrace/coef 通用路径验证）。
  > 注：本条目首次提交时**漏改了 `scripts/irt_common.R` 的校验白名单**，导致多维 GPCM（MGPCM）实际选了就报错退出，详见下方「修复（IRT 路径）」第 1 条。
- **自包含 HTML 网页版报告**（`scripts/generate_html_report.py`，三分支全覆盖）：每次分析完成时在结果目录生成 `stats_report_zh.html` / `ctt_report_zh.html` / `irt_report_zh.html`，并自动用默认浏览器打开（静默模式绝不弹窗，只在 complete 事件返回 `html_report` 字段）。**零外链**（无 CDN/字体/图表库，PNG 全部 base64 内嵌，可拷到离线电脑直接双击打开）；左侧固定导航滚动高亮、三线表逐行淡入、手写内联 SVG 图表（箱线图/直方图/碎石图/载荷热图/ICC/IIF/TIF/中介路径图）带 CSS 入场动画（柱生长/折线描出/热图浮现）、第 0 节数据健康卡（原始N→插补→删除→最终N 数字滚动）与均值插补"分布向中心收缩"教学动画、深色模式自动切换、窄屏导航折叠、表格首列冻结。中文叙事与表格匹配规则直接复用 Word 报告生成器，数值逐一核对一致；统计核心与既有产物（CSV/md/docx/manifest）零改动。
- **GUI 视觉升级**（WinForms 内，零新依赖）：集中主题表（深蓝主色系，22 个语义色，正文对比度 ≥ WCAG AA）替换全部散落色值；步骤面板卡片化；R/Python 状态灯（绿/黄/红圆点+文字）；长任务三段进度条（装 R 包→跑分析→出报告，随日志关键词推进）；完成后状态栏转绿+"打开中文报告"按钮温和脉冲；按钮分级（主操作实心/次要描边/危险弱化文字）。

### 修复（IRT 路径 · 全流程走查 + 新增 5 个模拟数据集）

- **GPCM / MGPCM 之前根本跑不起来**。`grep` 走查发现 `gpcm` 已在四个地方被承认：
  `irt_generic_pipeline.R` 的 `itemtype` 解析、`irt_preflight.R` 的取值判断、
  GUI 模型向导的「MGPCM：多维 GPCM」、终端「潜变量维度 → 多维 → 多维 GPCM」。
  唯独 `scripts/irt_common.R` 的配置校验白名单只写了 `auto/grm/2pl/3pl`，
  于是**只要选中多维 GPCM 就在读配置阶段直接退出**：
  `analysis.mirt_item_model must be auto, grm, 2pl, or 3pl.`
  两个入口都能产出该取值，属必现路径（真机复现：`2D EIFA-GPCM` 用例 EXIT=1）。
  已把 `gpcm` 补进白名单与报错文案；修复后同一用例正常完成（数据 → 拟合 → 双语文档 + 网页报告），
  模型名回显 `2D EIFA-GPCM`。
- **失败提示会把已经写清楚的原因替换成没信息量的套话**。`run_irt_analysis.ps1` 对体检返回的
  每一条 fatal 都再调用一次 `Translate-RFailure`；而体检给出的内容有两种：① `irt_preflight.R`
  直接写好的中文说明，② R 抛出的英文异常原文。对 ① 再翻译一遍会把它改写成不相关的文案
  （例：多级数据选 2PL 时，准确的"多级有序数据的 MIRT 应使用多维 GRM/GPCM"被换成通用的
  "R 分析未完成。请检查数据体检"），对 ② 若没匹配到任何模式也落到同一句兜底。
  新增 `Format-Fatal`：**含中文的原样透传**，英文异常才翻译，且译文落在兜底句上时回显原文。
  实测三条失败路径（二分+gpcm、多级+2PL、整列全缺失）现在都直接给具体原因。
- **GPCM 的题目参数表本身是错的（数值级）**。`irt_generic_pipeline.R` 用 `grep("^a", names(pars))`
  取判别力列，而 mirt 的 `coef()` 在 gpcm 下除 `a1..aD` 外还会带回**类别乘子列 `ak0..ak4`**（取值 0,1,2,3,4）。
  于是 `MDISC = sqrt(Σaⱼ² + Σak_k²)`：单维 gpcm 实测 **MDISC = 5.536，而该题 a 只有 0.805**；
  连带 `b_d*` 被同一个因子整体压错，`Primary_dimension` 还会输出 `ak4` 这种并不存在的维度。
  改为 `grep("^a[0-9]+$")` 后 MDISC 与 `sqrt(Σa²)` 的最大绝对差降到 5×10⁻¹⁶。
  同时修掉 b 列口径：GPCM 的 d_k 是**类别截距**（参考类别 d₀ ≡ 0），台阶难度是**相邻截距之差**
  `b_k = -(d_k - d_{k-1})/MDISC`；照搬 GRM 的 `-d_k/MDISC` 会得到"累积和"（既不单调、
  又多出一列恒为 0 的 `b_d0`）。现按台阶口径输出 `b_d1..b_d_{K−1}`，并与 mirt 自己的
  `coef(fit, IRTpars = TRUE)` 逐题比对：**最大绝对差 5×10⁻¹⁵**（修复前是数量级错误）。
- **新增 5 个模拟数据集，专门补齐此前没被任何数据覆盖的路径**（详见
  `examples/simulated_datasets/README_模拟数据说明.md`）：
  - **IRT-G**（三维 GPCM / MGPCM，900 × 24）：此前没有任何可直接试跑的 MGPCM 数据。
    实测 EIFA 台阶恢复 *r* = .995；CIFA 的 θ 逐因子恢复 *r* = .94/.93/.94（EIFA 只有 ~.4，因斜交旋转不可逐因子比对）。
  - **IRT-H**（二分 3PL，1500 × 20，c ∈ [0.08,0.25]）：实测忽略猜测按 2PL 拟合会把难度**平均低估 0.285 logit**；
    **逐题 c 无法恢复（*r* = −.406）**，且**并列比较表里 BIC 更低的是 2PL**（33561.2 vs 33641.5；配置显式写了 `model: 3pl`，
    故实际仍按 3PL 出结果，若交给 `auto` 就会被判成 2PL）——这正是"3PL 需要更大样本"的实测依据。
  - **IRT-I**（二维二分 / M2PL，800 × 20）：此前唯一的 MIRT 数据是多级 GRM，二分多维没有数据。
  - **CTT-D**（三因子近似正交，700 × 18）：覆盖 `rotation: varimax` 与手动 `n_factors`（原有三份 CTT 数据都是 promax + auto）；
    实测 **18/18 题结构完全恢复**。
  - **CTT-E**（脏作答 `remove` + 负向效标，800 × 15）：覆盖 `straightline_action/extreme_action: remove`
    与 `criterion_direction: negative`（原有配置全是 flag + positive）；实测 800 → 721（删 79 人）。
    顺带证实一条**真实现象**：k 题 K 点量表的总分 |z| 上限约为 `2/√(2r̄)`，
    α ≥ .70 时上限只有 ~3.0——这时把载荷调回 .55–.78，`|z|>3` 与 `1.5×IQR` **一个都命中不了**
    （实测 SD 13.4、两规则上界 91.0 / 91.5，而满分才 75）。故 CTT-E 刻意用低载荷（α ≈ .56）以让该分支真正被执行到。
- **生成器改成平台无关**：`w()` 原先直接用 `write.csv`，Windows 下文本连接会把 `\n` 落成 `\r\n`，
  于是"换个平台重跑生成器"会让 `git status` 一片 M（实测 24 个既有 CSV 全部变成 modified）。
  现在写完后统一把行尾转回 LF，**重新生成后既有文件字节一致**（已用 `diff -rq` 复核）。
- **新增 GUI 自检 G4 断言**：把"IRT 模型向导产出的每一个 `mirt_item_model` 都必须被 R 侧接受"
  变成断言，且允许值**直接从 `scripts/irt_common.R` 解析**（不另写一份，避免实现与测试再次漂移）。
  已做反向验证：把向导取值临时改成 `bogusmodel`，自检立即失败并指名
  `（允许：auto/grm/gpcm/2pl/3pl）`；上面那个 gpcm 漏洞若早有这条断言，提交时就会红。
- **`model_recommendation.csv` 的 `Selected` 列在显式指定模型时是错的**：该列原先按"BIC 最优那一行"标记，于是配置里写 `model: 3pl` + `compare_models: [2pl, 3pl]` 时，
  推荐表把 `2pl` 标成 `Selected=TRUE`，而实际拟合、报告与结果目录名用的都是 3PL——表与运行自相矛盾。
  现改为按**本次实际选中**的模型标记（EIFA 分支取 BIC 最优、CIFA 取 CIFA、其余取显式指定的模型）。
- **新增 smoke_test.ps1 的 GPCM 参数表护栏**（-Full）：跑一次单维 GPCM 端到端
  （`tests/e2e_gpcm.json`），断言 `MDISC` 等于 `sqrt(Σaⱼ²)`（表内恒等式，专门兜住 `ak*` 混入这一整类错误）、
  判别力列恰为 `a1`、台阶列为 `b_d1..b_d4` 且不出现 `b_d0`、所有 b 值有限。
  （该断言第一版写成 `[double]::IsFinite`，而 PowerShell 5.1 跑在 .NET Framework 上并没有这个方法，
  被自检自己抓出来后改用 `IsNaN`/`IsInfinity`。）

## [0.1.0] - 2026-09-17

> **Psychostat 的第一个版本，尚未对外发布。**
> 版本号由先前的 1.0.0 改回 **0.1.0**：本项目还没有发布过，1.0.0 会暗示"接口已冻结的正式版"，
> 与实际状态不符。
> 仓库早期的 0.1.0 / 0.2.0 / 0.3.0 三段只是**开发期内部迭代记录**（从未构建、从未对外发布），
> 为避免与本次 0.1.0 撞号，已把这三段的编号改为 **0.0.1 / 0.0.2 / 0.0.3**，内容一字未改。
> 0.1.0 的含义：**功能与统计口径已基本定型、输出接口稳定、已知限制已写入文档**，
> 但仍属发布前状态，输出文件名与配置键仍可能调整；残留待办见 「内部问题清单」。
> 破坏性变更（输出文件名、配置键、命令行参数）在正式发布后需升主版本号。

### 破坏性变更

- **输出文件名与生成脚本改名**：Word 报告的 "APA" 字样全部移除（本工具的 Word 报告并非严格 APA 结构）。
  调用方若按旧文件名取报告需同步修改：`stats_report_zh.docx` / `stats_report_en.docx` / `ctt_report_zh.docx` / `*_report_zh.docx` / `*_report_en.docx`。
  生成脚本：`generate_stats_report.py`、`generate_ctt_report.py`、`generate_result_reports.py`。
  静默模式 `complete` 事件的 `apa_note` 字段更名为 `report_note`，并补齐 CTT/IRT 两个分支。


### 测试（0.1.0 的核心改动）

- **新增"生产代码"数值断言**：此前的黄金值多由 `simulate_*` 生成的数据经 base R/`car` 复算，
  验的是数据生成器而非 `run_*` 管线——把事后比较改错测试依然全绿。本次新增第 6、7 节，
  **直接调用生产函数**并比对可独立复算的值：
  - `levene_spss()`：非平衡构造数据，精确解 `F = 150544/9727 ≈ 15.4769`（df 2,6）与两组 `F = 8/5`；
  - `anova_table_spss()`：非平衡 2×2 的 **Type III** 平方和 `SS_A=72.6 / SS_B=15.0 / SS_AxB=0 / SSE=5.0`（dfe=6），
    并断言其与 Type I 顺序平方和不同（该断言用于鉴别"退回 Type I"的口径错误）；
  - Welch df 与 Satterthwaite 闭式一致，且与合并方差 df 不同；
  - `posthoc_tables()`：Bonferroni p = min(1, LSD p × m)、LSD 各行 t 等于该配对的合并方差 t；
  - 球形校正的结构性质：`1/(k-1) ≤ GG ε ≤ 1` 且 `GG ε ≤ HF ε`（不锁具体值，避免包版本漂移误报）；
  - listwise 删除人数可计算且正确。
- **新增 IRT 端到端 CIFA 断言**：直接拟合 `examples/simulated_mirt_2pl_binary.csv`（400×12，F1/F2 各 6 题），
  断言 2D CIFA 的 BIC 明显优于单维（ΔBIC < −50）、ΔCFI > .10、CFI > .95 且 RMSEA < .08，
  并与文档记录的实测值（CFI≈1.000 / RMSEA≈0.000 / SRMSR≈0.033 / BIC≈5656.68）宽松比对。
  此前 `-Full` 自检对该分支只检查"文件是否存在"。

### 测试修正（0.1.0 第二轮自检反馈）

第二轮实跑仍有 5 条红，**全部是新增断言自身的写法问题**（生产代码与统计口径无问题）：

- **LSD 的 t 断言方法错误**（我的错）：测试里重设了随机种子后自己造数据，与模块内部 `posthoc_tables()`
  用的那份数据不是同一份，因此"另跑一次 `t.test` 再比 t"必然对不上（而同一行的 p 与 Bonferroni 断言
  依赖模块自身的 t，所以能过）。已改为**内部自洽性断言**：`t = 差值/SE`、`CI = 差值 ∓ t_crit·SE`，
  并新增 `Bonferroni 与 LSD 的差值/SE/t 逐行相同`。这类断言只依赖模块报出的数，与测试用哪份随机数据无关。
- **IRT 端到端改为断言"管线自己产出的"模型比较表**（此前是测试里另起炉灶重拟合，因模型语法构造不等价
  而让 2D 退化成更差的模型：BIC 2D=6110 vs 单维=5808.8，CFI=NA）。现在改为调用
  `run_irt_analysis.ps1 -Silent` 并读取结果目录里的 `*_model_comparison.csv`，用 `jsonlite` 正式解析
  `complete` 事件的 `data.result_dir`（不再手写正则）。同时明确：该配置走内置模拟数据（seed=20260815），
  与 `examples/simulated_mirt_2pl_binary.csv` 不是同一份数据，故断言**结构性关系**（ΔBIC < −20、ΔCFI > .10、
  CIFA 的 BIC 不得比单维差）而不是文档里那组具体数值。
- 新增"防退化"断言：**CIFA 的 BIC 不得差于单维**——这正是本轮踩到的坑（语法错误会让 2D 变成差模型），
  今后同类错误会被这条断言直接抓住。

### 已撤销：单尾检验与 α 选择（改为固定双尾 + α = 0.05）

曾加入单尾方向（`analysis.tail`）与显著性水平 α / 置信区间水平（`analysis.ci_level`）可选项，
因教学场景不需要而撤销，恢复固定双尾 + α = 0.05——各 t 检验的 `alternative` / `conf.level` 传参、
终端与图形界面的"检验设置"询问、报告中的单尾标注与 `tail_zh` / `tail_note` / `ci_label` 助手一律移除，
连同只覆盖该功能的第 9.1–9.4 条断言（它们其实只在测 base R 的 `t.test()`，留着即为死测试）一并删除；
统计口径与算法未做任何改动。第 9 节余下的断言改名为 9.1–9.3，全部是下面第 1 条的保留项。

撤销时**保留**了同批修掉的两处独立 bug（与单尾功能本身无关，故不回退）：

1. **事后比较的 CI 临界值写死 0.05**（`posthoc_tables()` / `paired_posthoc()` 不接收 alpha）
   → `alpha = .10` 时会出现"`p_adj` 显著却 CI 含 0"的自相矛盾。现在临界值跟随 `alpha`
   （`qt(1−α/2, dfe)` 与 `qt(1−α/(2m), dfe)`），3 个调用点传入 `alpha = ctx$alpha`。
2. **功效模块的样本量表题与正文写死**"α = 0.05"与"每组约 394 人" → 改为读取实际 α 与表内实际值。

### 修复（重要）：调节效应的 X/Z 根本没有做均值中心化

用户对照 `SPSS与Psychostat心理统计模块输出对应.docx` 发现调节效应的 4 个系数都与 SPSS PROCESS 对不上，
怀疑"R 没做中心化"。核查后确认**确实是**，而且比表面更严重：

- 根因：`run_moderation()` 里写成了 `scale(x, scale = FALSE, center = FALSE)` ——
  `center = FALSE` 等于**根本不做中心化**（Xc 的均值实测为 35.7，而不是 0）。
- 后果：**4 个系数与 4 个标准误全部失真**。截距退化成"X = 0 且 Z = 0 时的预测值"
  （教学数据上为 2.308，正确值为 30.513），两个主效应也随之偏移，并因与乘积项严重共线，
  X 主效应的 SE 由 .067 被抬到 .198；
  只有**交互项恰好不受中心化影响**（中心化是线性重参数化），所以只核对交互项会漏掉这个 bug。
- 修法：改为显式均值中心化 `Xc = X - mean(X)`、`Zc = Z - mean(Z)`，与 SPSS PROCESS 的 "Mean center" 口径一致。
- 验证（真实教学数据 `11_相关与偏相关分析数据.csv`，n = 120，与该 docx 中 SPSS PROCESS 输出逐项对账）：
  SPSS `constant = 30.5128 / x = .3158 (SE .0671) / w = .5825 (SE .0845) / Int_1 = −.0018 (p = .7987)`，
  Psychostat 修复后为**完全相同的四位小数**，R² = .5085、F(3,116) = 40.0019 亦一致。
- 新增第 10 节共 11 条断言锁住该口径：4 个系数与 SE 与独立复算的中心化模型一致（tol 1e-8）、
  截距恒等式、与"未中心化"口径的判别式、SE 回落、简单斜率两式。
  **注意恒等式的正确形式**：X、Z 中心化后 `截距 = mean(Y) − b₃·mean(Xc·Zc)`；
  只有 X 与 Z 不相关时 `mean(Xc·Zc) = 0`，它才退化为"截距 = 因变量均值"。
  测试数据特意让 X 与 Z 相关（r ≈ .45），以免又退回到那个只在特例下成立的弱断言。

### 修复（重要）：用真实数据跑相关分析必然失败、报告完全不生成

用户反馈"选相关检验（第 11 个方法）结束后没有跳出网页，报告好像也没输出"。核查后确认**两个症状同一个根因**，
且该 bug 自初始提交 `4627a8b` 起就存在：

- 根因：`stats_modules.R` 的 `run_correlation()` 里 `if (...)` **少了一个 `{`**，
  守卫只作用到紧随的一行：
  ```r
  if (all(c("study_hours", "final_grade") %in% names(X)))   # 只保护下一行
    .v1 <- names(X)[1]; .v2 <- names(X)[2]
    .r <- ...                                               # 无条件执行
    guide("...", .v1, ...)                                  # 无条件执行 → .v1 未定义
  ```
  该模板句只适用于内置模拟数据的固定列名，因此**内置模拟数据（列名恰好是 `study_hours` / `final_grade`）
  永远走不到坏分支**——只有用真实数据文件才会触发 `object '.v1' not found`。
- 后果链：R 以退出码 1 结束 → `psychostat_gui.ps1` 用 `$code -eq 0` 守卫整个报告块
  → Word 报告、HTML 报告、自动打开浏览器**整块被跳过**；`-Silent` 路径更早就 `Stop-Friendly` 退出。
  所以"没生成报告"与"没跳转"是一件事，不是两个 bug。
- 修法：补上 `{` 与配对的 `}`，并加注释说明为何必须整块受守卫。**纯控制流修复，统计口径零改动**
  （守卫为真时执行的语句集与修复前逐字相同）。
- 验证：同一份真实列名数据 A/B 对照——修复前 `ERROR: object '.v1' not found`，修复后 `SUCCESS`；
  `-Method correlation` 端到端 21/21 方法、0 失败，HTML 报告含完整相关分析节（两两相关表 + 4 个矩阵 + 散点图 + 叙事）。

### 新增：数据准备与异常值筛查（第 0 步）

补齐了三个分支里最后一块短板——**心理统计分支此前没有数据清洗**（21 个方法静默整例删除，不报告删了几人）。

- **新增模块 `datacheck`（默认自动运行，不占用方法编号）**，任何分析前自动运行，产出四组表：
  ① 缺失值审计（逐变量缺失数/比例、个案级缺失清单、整例删除影响）；
  ② 异常值筛查（**Z 分数**与 **1.5×IQR/3×IQR** 双标准，出汇总表与个案清单）；
  ③ 数据完整性检查（ID 重复、整行重复、常量列、取值越界——含"缺失码 99 被当成真实分数"的识别）；
  ④ **数据准备摘要**（原始 N → 处理 → 最终 N），可直接引用到论文方法部分。
- **交互询问**：终端与图形界面都会先报告问题、再问用户要不要处理；**直接回车 = 不处理**。
  处理方式由配置决定（默认 `report`，只报告不改动数据）：
  `datacheck.missing_action`（report | **mean_impute 均值插补** | listwise）、
  `datacheck.outlier_action`（report | **remove_by_z 按 |Z| 删除**）、
  `datacheck.z_cutoff`（默认 3）、`datacheck.scale_range` / `datacheck.scale_vars`（量表范围与哪些变量按量表计分）。
- **不再出现在"选择统计方法"页**：这一步是**每次分析都要做的前置检查**，与前面几个前置检查重复，
  因此终端与图形界面的方法清单里都不再列出它（原本作为第 22 个可选方法列在第一位）。
  它仍然**无条件自动运行**，也仍可通过 YAML 显式指定 `method: [datacheck]` 单独跑一遍数据体检。
- **固定处理顺序**：先算 Z → 先删异常个案 → 再对剩余数据均值插补。
  若先插补，插补值会把均值拉向中心、人为缩小 SD，使真实极端值"逃过"Z 检验。
- 全部处理过程落盘可追溯（`00_prep_*` 表 + 报告摘要），**绝不静默丢弃数据**。
  前缀用 `00_prep_` 而非 `00_data_`，是为了与"把 `datacheck` 当独立方法选跑"时的方法编号互不覆盖。
- 新增第 8 节共 14 条断言（缺失计数、listwise 影响、均值插补的均值不变与方差缩小、
  Z 规则的理论上限 (n−1)/√n、IQR 命中、删除集合口径、ID 重复/常量列/越界与缺失码识别、演示数据确实含问题）。

### 测试修正（0.1.0 首轮自检反馈）

首轮实跑 `run_numeric_tests.ps1` 暴露 5 条**新增断言自身**的写法错误（不是生产代码问题，Type III / Levene / Welch 全部一次通过）：

- **`posthoc_tables()` 的列名是 `p_adjusted` 而非 `p`**：改为按实际列名断言，并补三条更有价值的检查
  （LSD 的 p 为未校正双尾 p、Bonferroni p = min(1, 原始 p × m)、Bonferroni 的 CI 不窄于 LSD）。
- **球形校正改走生产函数**：不再直接猜 `car::Anova` 返回对象的结构，改用 `spss_repeated_glm()` + `gg_hf_epsilons()`，
  断言 `1/(k-1) ≤ GG ε ≤ 1` 与 `GG ε ≤ HF ε`。
- **IRT 端到端改用管线自身的取指标方式**：`mirt::fitMeasures` 并非导出函数，改为照抄 `fit_statistics()` 的
  `M2(type="M2*")` + `fit@Fit$BIC`；模型语法不再用 `mirt.model(list)` 而按管线形式给出 `F1=…\nF2=…\nCOV=F1*F2`
  （此前触发 `model.elements` 的 coercion 警告并导致拟合失败）。M2 取值加了 S4-safe 保护。
- 该节要真拟合两个 2PL 模型（数十秒），可用环境变量 `PSYCHOSTAT_SKIP_IRT_FIT=1` 跳过。

### 修复（0.1.0 追加）

- **修掉一处我引入的 R 运行期错误**：`irt_generic_pipeline.R` 的载荷矩阵构造里 `check.names = FALSE` 重复传入
  （R 中重复形参是 Error），已在改名过程中产生。
- **`tests/smoke_test.ps1` 改用产品自身的四级 R 发现逻辑**（`Initialize-PsychostatEnvironment`，D 盘 → PATH → 注册表 → C 盘），
  不再只用 `Get-Command Rscript.exe`：否则"D 盘装了 R 但没进 PATH"这一产品明确支持的场景下，冒烟测试会误报失败。
- **删除 3 个确认零调用的死函数**：`guide_cat()`、`report_heading()`、`fmt_num()`（`scripts/stats_common.R`）。
- **删除一处不可达分支**：调节模块中 `need_var()` 之后的重复存在性检查（`need_var` 已在列不存在时 `stopf`）。
- **新增 `CONTRIBUTING.md`**：说明本项目的开发约定、PowerShell 5.1 专项坑、两类断言的取舍、
  以及补 SPSS 夹具的流程（`tests/fixtures/spss/` 至今仍是空骨架）。

### 项目整理（发布准备）

- **删除重复/过期文档**：`docs/Psychostat_CTT使用说明.docx`（与 `docs/CTT使用说明.md` 重复且标注 0.1.0）及其生成器 `scripts/generate_ctt_user_guide.py` 一并移除；「内部问题清单」 只保留 Markdown 源，其 Word 版改为可再生产物、不再入库（`.gitignore` 增加 `docs/*.docx`）。根目录改为只放三份 Word：`Psychostat完整使用说明书.docx`、`Psychostat使用说明.docx`、`SPSS与Psychostat心理统计模块输出对应.docx`。
- **IRT 说明书清理**：`docs/IRT工具使用说明.docx` 去掉指向旧工程 `05_General_IRT_Tool` 的表述，并把依赖清单更正为启动器实际安装的 `yaml / jsonlite / mirt / psych / ggplot2 / readxl / haven`（原列出的 `openxlsx / knitr / rmarkdown` 早已移除）。
- **`SPSS与Psychostat心理统计模块输出对应.docx` 补"校对范围与已知差异"开头**：明确它覆盖 20 个模块（未含功效分析）、第四节独立样本 t 检验缺 SPSS 侧输出、混合设计仍有 3 处未逐位对齐（EM 边际均值 SE、time 配对比较误差项、Huynh-Feldt ε），并说明该文件不随 CI 运行、不能防回归。README"已知限制"第 2 条同步改写（不再说"没有外部基准"，改为说明该校对的覆盖范围与残余差异），`renv.lock` 的定位也改为"参考快照，非已验证的复现路径"。
- **文档生成器**：`generate_docs.py` / `merge_manuals.py` 增加 `save_docx()`（先写临时文件再原子替换，避免部分文件系统上原地保存失败）。

### 变更

- **全面移除 "APA" 字样**：本工具的 Word 报告并非严格 APA 结构，为避免误导，项目内所有 "APA" 字样（文案、文件名、函数名、标识符、文档与 Word 说明）已统一改为中性表述（"结果报告 / 结果写法 / 三线表"）。**报告内容、结论与表格一律保留**，仅改称谓；输出文件名相应变更：
  `stats_report_zh.docx` / `stats_report_en.docx` / `ctt_report_zh.docx` / `*_report_zh.docx` / `*_report_en.docx`。
  生成脚本改名：`generate_stats_report.py`、`generate_ctt_report.py`、`generate_result_reports.py`（旧名 `generate_*_apa_report*.py` 已不存在）。
  静默模式的 `complete` 事件中 `apa_note` 字段更名为 `report_note`，并补齐 CTT/IRT 两个分支（此前只有心理统计分支带该字段）。

### 修复

- **结果示范句不再写死方向/构念名/显著性（重要）**：21 条"结果写法"示范句此前把结论写在字符串里、数字却来自用户数据，实测已产出与数据矛盾的句子（例如交互项 p = .900 仍写"效应受调节"、实验组均值更高却写"显著低于"）。现在全部改为**数据驱动模板**：方向由实际均值/差值/系数符号决定，多组与交互情形不再断言具体方向而是提示读者对照表格确认，p 未达显著时明确改写为"无统计学意义"的表述。
- **正态性结论不再把"未检验"当成"检验不显著"**：S-W 在 n < 3 或 n > 5000 时不计算（大样本问卷很常见），此前会输出"S-W 均 p > .05，可使用参数检验"的假结论。现在检验表新增 `sw_tested` 列，结论区分【未检验】/【显著偏离】/【未见偏离】三种状态，未检验时明确提示改看 K-S、偏度峰度与分布图。
- **第 0 步前置检查此前从未真正分组**：`preflight_columns` 返回的是分组变量的**列名**，`as.factor(列名)` 只有 1 个水平 → 按组描述统计与按组正态性检验全部退化为整体（产物表现为 segment 写作"变量名 · 组名"、N 为全样本）。现改为取实际列值。
- **单因素 ANOVA 报告层方差不齐不再无提示**：模块自身会在 Levene 显著时改用 Welch 行，但 Word 报告不读 Levene/稳健检验表，方差不齐时仍只报合并方差的 F。现在报告读取 `one_way_anova_levene` 与 `one_way_anova_robust_tests`，Levene p < .05 时追加明确警示并列出稳健检验结果。
- **中介 CI 计算失败不再被说成"中介不显著"**：报告层此前把 NaN 当作"CI 含 0"，把"算不出"渲染为"未发现中介效应"。现在区分两者，CI 不可用时明确写"无法计算，不能据此判断"。
- **列名含 `-` / 空格 / 顿号 / 括号 / 前导数字时不再静默出错**：用户问卷表头（如 `T1-T3总分`、`1、我常感到紧张`）此前被直接拼进 `as.formula()` 与 lavaan/mirt 语法——`-` 会被当作运算符导致模型静默变形，其余非法名直接报错。回归、分层回归、调节、CFA、CIFA 现在统一在内部改用 ASCII 安全名建模，输出表按原始列名回显；README 中"支持特殊字符列名"的承诺至此才真正成立。
- **启动器在 `$ErrorActionPreference='Stop'` 下用 `2>&1` 调用 Python**：PowerShell 5.1 会把原生程序 stderr 升级为终止性错误，导致"Word 报告生成失败"这一本该优雅提示的场景反而以原始报错崩溃。CTT/IRT 两个启动器已按心理统计分支的既有写法加保护。
- **GUI"检查环境/自动安装"必然挂起**：该按钮以隐藏窗口 + 重定向启动后台任务，而安装流程会 `Read-Host` 询问是否安装 R/Python——提示写进重定向文件，用户既看不到也无法回答，任务永久停在提问处。现改为弹出**可见**控制台窗口执行交互式任务。
- **Python 依赖探测早于 `PYTHONUSERBASE` 设置**：依赖由本工具装进专属目录时，每个新进程的首轮探测都看不到它们 → 每次运行重跑 pip，离线时还会把完好的依赖判为未就绪而丢掉 Word 报告。现在解析顺序与安装目录在首轮探测前即确定。
- **Rasch 模型可能把 ±Inf 写进题目参数表**：斜率固定的模型 `coef()` 可能不返回 a 列，此时 `MDISC = 0` 使 `b = -d/MDISC` 变成 ±Inf。现在缺 a 列时显式补 `a = 1` 并断言 `MDISC > 0`，异常值降级为 NA 并给出警告。
- **行内 p 值不再打印 `p = .000`**：示范句与解读文字统一走"p < .001"；CSV 结果表仍保留 SPSS 口径的 `.000`（以便与 SPSS 输出逐位对照）。
- **文档与实现不一致处已同步**：手册第 11.1.8 节称"没有 McDonald's ω / α 置信区间 / CFA 修正指数"（实际均已实现）、第 8.1 节称"CTT/IRT 没有配置硬校验"（实际三支都有）、CTT 与 CFA 两段同类陈述、以及 README 与分支说明中"小数点后 2–3 位完全一致"的过度声称（改为"代码级口径对齐，尚无外部基准夹具，混合设计有已知差异"）；补齐了被三处引用却缺失的 「内部问题清单」；README 新增"已知限制"章节；修正"Python 本体不会被自动下载安装"（实际会询问并安装）与"首次运行 1–2 分钟"（国内实测需数十分钟）两处不实表述。

- **Windows 首次配置环境的下载慢 / 装不完（试用者实测"下载 2000 多秒仍未完成"）**：
  - **R 包安装改用国内镜像并支持自动换源**：此前三个启动器与图形界面都写死 `repos='https://cloud.r-project.org'`（境外官方源），国内实测常只有几十 KB/s，而 psych / lavaan / mirt 及其依赖合计数百 MB。现在统一走共享函数 `Get-PsychostatPackageInstallCode`：**清华 → 中科大 → 阿里云 → 官方源**逐个尝试，某个包在一个源失败就自动换下一个源；`options(timeout)` 由默认 60 秒放宽到 1800 秒；Windows 下显式 `type='binary'` 直接装二进制包（不编译、不需要 Rtools）；**逐个包打印进度与耗时**，不再静默等待；已装的包自动跳过，因此中断后重跑是"接着装"而不是从头再来；全部失败时打印仍缺的包名与三条可操作建议。
  - **安装脚本改为临时文件方式执行**：多行 R 代码经 PowerShell 传给 `Rscript -e` 会被拆坏（实测报 `unexpected end of input`），故新增 `Get-PsychostatPackageInstallScript`——把安装逻辑写到 UTF-8 无 BOM 的临时脚本 `%TEMP%\psychostat_install_packages.R` 再执行（排错时可直接查看/回传该脚本）。四个调用点（`run_stats_analysis.ps1`、`run_ctt_analysis.ps1`、`run_irt_analysis.ps1`、GUI 的后台任务模板）统一改为调用共享函数。
  - **R / Python 安装包下载器重写**：不再用 `Start-BitsTransfer`（低优先级后台传输，卡住时既无进度也无超时）。新增 `Save-PsychostatFile`：显式启用 TLS 1.2（PS 5.1 默认可能只协商 TLS 1.0/1.1，部分镜像会直接拒连）、**每 2 秒刷新进度（已下载 MB / 百分比 / 速度 / 预计剩余秒数）**、单次读取超过 30 秒无数据即判定卡死并放弃该源、下载不完整或体积明显偏小（像是镜像的错误页）即判失败、失败自动换下一个镜像；HttpWebRequest 失败时回退 WebClient 再试一次。R 本体镜像由 2 个扩到 4 个（清华 / 中科大 / 阿里云 / 官方）。
- **发送包 / 试用包的解压根目录修复**：打包脚本用 `$env:TEMP` 的 **8.3 短名**长度去截取相对路径，而 `Get-ChildItem` 返回的是**长名**（中文用户名在临时目录里会被系统压成 8.3 短名，两者长度不同），长度差导致 zip 内路径从 `Psychostat/…` 被啃成 `t/…`：解压出来是一个名为 `t` 的文件夹，与说明书里"打开项目文件夹 Psychostat"对不上。现按 `Get-Item` 规范化后的全路径截取，统一包一层 `Psychostat/`，并在打包末尾**断言 zip 顶层目录必须含 `Psychostat/` 且不得出现 `t/`**。已用"解压 → 跑冒烟自检"复验：发送版 129 个文件、试用包 130 个文件，均 `ALL CHECKS PASSED`。
- 启动器与 R 管线的 P0 级健壮性：输入路径校验、IRT 数据全缺失行的处理、IRT 维度启发式补齐 `psych` 依赖（fa.parallel/tetrachoric）、CTT 说明中 semPlot 仅作 CFA 路径图可选依赖的提示。
- 统计口径：信度报告补充 ω 系数与置信区间；CFA 合并方差（CR）计算口径修正。
- GUI：后台任务脚本模板对含撇号路径（如 O'Brien 用户目录）做单引号转义，不再产生语法错误。
- 敏感数据检查（check_sensitive.ps1）扩展到已跟踪文件：干净检出（CI）下也能发现误入库的真实数据。
- **CTT EFA 单因子解（单维量表）的建议删题修复**：`primary_loading` 不再退化成常数，不再把干净题目误判为交叉载荷，低载荷判定恢复生效（此前 `apply` 在 nf=1 时退化，`vapply` 同样存在该陷阱，故改为显式分支）。
- **中介模块完全共线崩溃修复**：自变量与中介变量完全共线（如同列重复/线性复制）时，`summary()` 会整行剔除共线系数导致 `cb["M", ]` 下标越界、整条管线中止且无 Word 报告；现在拟合前做可识别性预检（有效 n、常量列、|r(X,M)|>0.9999）并给出可操作的中文报错，拟合后对系数行缺失做防御。
- **管线单方法失败容错**：多方法运行时某个方法报错不再中止整次运行——其余方法照常产出，报告（md 与 Word）在对应章节和摘要标注「未能完成」及原因；仅当全部方法失败时才以非零退出。
- **题目列自动识别的退化列不再静默丢弃**：整列全缺失 / 零方差的列会显式提示并计入 IRT 数据体检的 `excluded_degenerate_columns` 字段；显式列出题目列时，"整列全缺失"优先报错并给出**正确列名**（此前报"非计分类型（因子/逻辑）：NA"）。
- **注意检验类列纳入自动识别守卫**：`check|attention|verify|careless|valid` 与中文 `注意|检验|甄别|作答`，避免未声明 `attention_item` 时把检验题当成一道量表题目纳入信度与 EFA。
- **GUI 的 Word 结果报告统一 Python 解析逻辑**：此前 GUI 用自己的 `Get-PythonPath`（PATH 优先、命中 `py.exe` 直接用转发器、且不校验依赖），与环境检查按钮/命令行启动器那套（`Initialize-PsychostatPythonEnvironment`：校验 pandas/python-docx、缺依赖自动安装、设置 `PYTHONUSERBASE`）不是同一个解释器——在多 Python 的机器上会报 `ModuleNotFoundError: No module named 'pandas'`，而"检查环境/自动安装"却显示就绪。现在生成脚本内部调用同一函数解析，并打印实际使用的解释器路径；跳过时给出具体原因。界面状态栏的 `py.exe` 也会解析成真实解释器路径。

### 新增

- **环境清理功能（面向"只用一次"的使用者）**：`scripts\uninstall_psychostat.ps1` —— 默认只**预览**（列出将删除的目录、体积，以及明确不删的项目及原因），加 `-Execute` 才真正执行并二次确认。只删除本工具**专属**目录（`Psychostat-R-Library`、`Psychostat-Python-User`、`PsychostatTemp`）与安装清单；目录名不在白名单、或位于项目目录内的一律拒绝（显示 `[拒绝]`）。**不删除**已存在的 R/Python 本体、共享的 R/Python 用户库，也不删除 `outputs\` 与 `data\`（只列体积提醒）。Windows 图形界面左导航另有「清理已安装环境」按钮（可滚动预览 → 确认 → 执行）。
- **安装清单**：每次运行把"装了什么、装在哪、是否由本工具安装"记录到 `%LOCALAPPDATA%\Psychostat\install_manifest.json`（可用环境变量 `PSYCHOSTAT_STATE_DIR` 覆盖；清单**不随项目文件夹分发**），清理脚本据此精确删除，并兼容"本功能上线前安装过的机器"（无清单时按已知候选位置探测）。同时新增"未能创建专属 R 包目录"的提示——此前会静默退到共享用户库。
- `examples/simulated_datasets/`：9 个可直接试跑的模拟数据集（CTT 3 个、IRT 6 个）+ 生成真值（题目参数 / 维度归属 / 真能力 θ）+ 可直接使用的 YAML 配置 + 说明文档与练习题。
- `scripts/generate_simulated_datasets.R`：数据与真值生成器（支持 `--seed` / `--out`）。
- `docs/Psychostat_完整使用说明书.md` 与根目录 `Psychostat完整使用说明书.docx`：面向新手与研究者，含 21 个统计模块的方法注释、CTT/IRT 参数说明、结果写作模板与已知限制。
- `scripts/generate_docs.py`：把说明文档渲染为 Word（复用 `merge_manuals.py` 的渲染器）。
- 「内部问题清单」：代码审查问题清单（P0–P3，逐条含复现、证据、建议修法与验收标准）。可选/开发用文档，不存在时 `generate_docs.py` 会跳过而不是报错。

### 变更

- **移除 macOS 适配层，专注 Windows**：删除 `mac/`（psychostat_mac.sh、README_mac.md）、全部 6 个 `.command` 双击入口与 `scripts/psychostat_gui_mac.py`（Mac 图形界面）；README、CHANGELOG 与完整使用说明书同步改为仅 Windows 说明（`.bat` 在 macOS 上无法运行，会被系统交给抢注该类型的其它 App）。统计核心（R/Python）本就与平台入口无关，未做任何改动。


- 发布要素：新增 CITATION.cff 与本 CHANGELOG；`.gitattributes` 补充编码约定说明（ps1/bat 为 UTF-8 带 BOM，其余文本无 BOM）；`启动AI智能体工作流.bat` 精确定位 AI 工作流文档。
- 项目结构整理：内部工作笔记移入 `notes/`（已被 `.gitignore` 忽略）；项目文档统一到 `docs/`；README 的"项目结构"章节同步更新。

### 测试

- 数值回归与冒烟测试增强，覆盖上述修复。
- 冒烟自检新增"文件编码约定"检查：`*.ps1` 必须带**恰好一个** UTF-8 BOM、`*.bat` 必须纯 ASCII、`*.R`/`*.py` 必须无 BOM。此前这类问题不会被任何检查发现，而它一旦发生（编辑器重写脚本时丢掉 BOM），PowerShell 5.1 会按本地代码页解析中文 .ps1，直接导致界面报语法错误或乱码。

## [0.0.3] - 2026-09-12

### 新增

- 向导式图形界面（WinForms，零依赖）：左侧分支/步骤导航，逐步收集参数后调用既有 R 管线；任务后台运行、日志实时刷新、心跳与取消；深度自检（布局、步骤切换、防遮挡、分析级端到端）。
- 10 个统计模块补充教学图（t 检验、卡方、非参数、功效等）。

### 修复

- 代码评审问题 S1–S9 及 GUI/结果报告多项改进（数值回归 21/21 通过）。

## [0.0.2] - 2026-09-10

### 新增

- 首次运行自动安装缺失环境：R（镜像发现 + 静默安装）、Python（多镜像下载）及依赖（pip 走清华镜像并显示进度）。
- macOS 支持：`.command` 启动器与 `mac/psychostat_mac.sh` 引导脚本。
- 各分支按需安装 R 包（不再装孤儿包）；SPSS 自带 Python 3.8 不再劫持结果报告的依赖（候选解释器 ≥3.9）。

### 修复

- 启动器健壮性：ASCII 批处理（不依赖代码页）、D 盘优先环境发现、无 R 机器上的版本选择崩溃、缺包时友好报错。

## [0.0.1] - 2026-09-06

### 新增

- 首个可用版本：三分支心理测量工具箱（心理统计 / CTT / IRT），MIT 许可证。
- 一键启动器、模拟数据演示、Word 结果报告、运行清单（run_manifest.json）与依赖锁定（requirements-lock / renv.lock）。
- 私有数据保护：敏感文件提交前检查（.sav/.xlsx/.csv 白名单豁免）。
- CI（Windows）：数值回归测试 + 三分支冒烟测试。
